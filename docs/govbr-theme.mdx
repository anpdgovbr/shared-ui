import { Meta } from '@storybook/blocks'
import { Button, Typography, Switch, Card, Link, Divider } from '@mui/material'
import { GovBRThemePreview } from './GovBRThemePreview'

<Meta title="Documentação/Tema GovBR-DS" />

# Tema GovBR-DS para Material-UI

Este documento detalha a implementação do **Sistema de Design do Governo Federal (GovBR-DS)** como
um tema customizado para o **Material-UI (MUI)**. Nosso objetivo é garantir que todos os
componentes MUI utilizados em nosso projeto estejam em total conformidade visual e funcional com
as diretrizes do GovBR-DS, proporcionando uma experiência de usuário padronizada e acessível.

---

## Instalação e Uso Rápido

```
npm install @anpd/shared-ui
```

```tsx
import { govbrTheme } from '@anpd/shared-ui'
import { ThemeProvider } from '@mui/material/styles'
;<ThemeProvider theme={govbrTheme}>{/* sua aplicação */}</ThemeProvider>
```

💡 Este tema é compatível com as variáveis CSS do GovBR-DS (`core.min.css`, `core-tokens.min.css`).  
Quando essas folhas de estilo estiverem carregadas globalmente, os componentes se ajustam automaticamente aos tokens visuais da identidade institucional.

---

## Paleta de Cores

<GovBRThemePreview>
  <Button variant="contained" color="primary" sx={{ mr: 2 }}>
    Primary
  </Button>
  <Button variant="contained" color="secondary" sx={{ mr: 2 }}>
    Secondary
  </Button>
  <Button variant="contained" color="success" sx={{ mr: 2 }}>
    Success
  </Button>
  <Button variant="contained" color="warning" sx={{ mr: 2 }}>
    Warning
  </Button>
  <Button variant="contained" color="info" sx={{ mr: 2 }}>
    Info
  </Button>
  <Button variant="contained" color="error">
    Error
  </Button>
</GovBRThemePreview>

---

## Tipografia

<GovBRThemePreview>
  <Typography variant="h1" gutterBottom>
    H1 - Título Principal
  </Typography>
  <Typography variant="h2" gutterBottom>
    H2 - Subtítulo
  </Typography>
  <Typography variant="h3" gutterBottom>
    H3 - Seção
  </Typography>
  <Typography variant="body1" gutterBottom>
    Este é um parágrafo de texto para demonstrar a tipografia base. Ele segue o `body1` do MUI, que
    é mapeado para o estilo padrão de parágrafos do GovBR-DS.
  </Typography>
  <Link href="#" variant="body1">
    Link Exemplo
  </Link>
</GovBRThemePreview>

---

## Switch (Estados e Tamanhos)

<GovBRThemePreview>
  <Switch defaultChecked sx={{ mr: 2 }} />
  <Switch sx={{ mr: 2 }} />
  <Switch size="small" defaultChecked sx={{ mr: 2 }} />
  <Switch size="large" sx={{ mr: 2 }} />
  <Switch disabled defaultChecked />
</GovBRThemePreview>

---

## Card com Sombra e Hover

<GovBRThemePreview>
  <Card
    sx={{
      p: 3,
      maxWidth: 300,
      '&:hover': {
        backgroundImage: 'linear-gradient(rgba(51,51,51,0.16),rgba(51,51,51,0.16))',
      },
    }}
  >
    <Typography variant="h5" sx={{ mb: 1 }}>
      Título do Card
    </Typography>
    <Typography variant="body1">
      Este é um exemplo de card. A sombra e o efeito de hover são customizados para o estilo
      GovBR-DS.
    </Typography>
    <Button variant="outlined" sx={{ mt: 2 }}>
      Saiba Mais
    </Button>
  </Card>
</GovBRThemePreview>

---

## Divider (Horizontal e Tracejado)

<GovBRThemePreview>
  <Typography variant="body1">Conteúdo acima da divisória.</Typography>
  <Divider sx={{ my: 2 }} />
  <Typography variant="body1">Conteúdo abaixo da divisória.</Typography>
  <Divider sx={{ my: 2 }} variant="fullWidth" />
</GovBRThemePreview>
